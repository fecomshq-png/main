/* ============================================================
   THE ONLY FILE YOU NEED TO EDIT.

   Paste the Apps Script web-app URL here. It ends in /exec.
   See README.md, step 3.

   There is deliberately NO admin passcode in this file. The
   passcode lives only in the Apps Script (ADMIN_KEY) and is
   typed in when you click "Practice access" — so anyone who
   views the page source still cannot read the responses.
   ============================================================ */
window.API_URL = "PASTE_YOUR_WEB_APP_URL_HERE";
